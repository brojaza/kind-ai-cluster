.DS_Store
*.bak
*.swp
.idea/
.vscode/
*.tgz
.claude/

# Terraform - .terraform.lock.hcl IS committed (pins provider versions), everything
# else here is local state/cache that shouldn't be.
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan
terraform/crash.log
