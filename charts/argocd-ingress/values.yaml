# Default values for the argocd-ingress chart. See README.md at the repo root for
# context ("Host-based routing").

# Requires an /etc/hosts entry (or real DNS on a non-local cluster) pointing this host
# at 127.0.0.1 - see README.md. Settable via prereqs/.env's ARGOCD_HOST - see
# prereqs/apply-env.sh. The trailing comment below is that script's substitution
# anchor; keep it on the same line as `host` if you edit this by hand.
host: argocd.company.test # PREREQS:ARGOCD_HOST
