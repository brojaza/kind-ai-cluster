Open WebUI is starting.

{{- if eq .Values.service.type "NodePort" }}

This project's cluster-setup/scripts/kind-config.yaml maps host port 8080 -> nodePort {{ .Values.service.nodePort }}.
Once the pod is Ready, open:

  http://localhost:8080

{{- else }}

Access it with:

  kubectl -n {{ .Release.Namespace }} port-forward svc/{{ include "open-webui.fullname" . }} 8080:{{ .Values.service.port }}

then open http://localhost:8080

{{- end }}

The first account you register becomes the Open WebUI admin. The model will appear in
the picker as whatever charts/vllm's values.yaml sets for model.servedName once the
vllm pod is ready.
